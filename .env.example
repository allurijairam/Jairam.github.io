# Backend API base URL for upload (no trailing slash).
# Example for EC2: http://YOUR_EC2_PUBLIC_IP:8000
VITE_API_BASE=http://localhost:8000

# Chat API URL (POST JSON { "question": "..." } → { "answer": "...", "session_id": "..." }).
# Default: https://oald5wjp5c.execute-api.us-east-1.amazonaws.com/chat
# VITE_CHAT_API_URL=https://oald5wjp5c.execute-api.us-east-1.amazonaws.com/chat
